# Copy this file to .env.local to override defaults

# If you want to point the frontend at a backend running somewhere
# other than http://localhost:8000 (with Vite proxy), set this:
# VITE_API_BASE=https://api.your-domain.com
